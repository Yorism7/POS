# 📖 คู่มือตั้งค่า Streamlit Cloud + Supabase (ภาษาไทย)

## 🎯 เป้าหมาย
ตั้งค่าให้ระบบ POS ทำงานบน Streamlit Cloud และเก็บข้อมูลถาวรใน Supabase (ฟรี 100%)

## ⏱️ ใช้เวลา: 10-15 นาที
## 💰 ราคา: ฟรี 100%

---

## 📋 ขั้นตอนที่ 1: สร้าง Supabase Database (5 นาที)

### 1.1 ไปที่ Supabase
1. เปิดเบราว์เซอร์
2. ไปที่ **https://supabase.com**
3. กดปุ่ม **"Start your project"** หรือ **"Sign in"**

### 1.2 สร้าง Account (ถ้ายังไม่มี)
1. กด **"Sign up"**
2. เลือกวิธี Sign up:
   - **แนะนำ: Sign up with GitHub** (ง่ายที่สุด)
   - หรือใช้ Email
3. อนุญาตการเข้าถึง (ถ้าใช้ GitHub)

### 1.3 สร้าง Project ใหม่
1. หลังจาก Sign in แล้ว จะเห็นหน้า Dashboard
2. กดปุ่ม **"New Project"** (มุมขวาบน)
3. กรอกข้อมูล:
   - **Name**: `pos-database` (หรือชื่อที่ต้องการ)
   - **Database Password**: 
     - ตั้งรหัสผ่านที่แข็งแกร่ง
     - **⚠️ จำไว้ให้ดี!** (จะใช้ตอนตั้งค่า Streamlit Cloud)
     - ตัวอย่าง: `MySecurePassword123!`
   - **Region**: 
     - เลือก **Southeast Asia (Singapore)** (ใกล้ที่สุดสำหรับไทย)
     - หรือเลือก region ที่ใกล้ที่สุด
   - **Pricing Plan**: เลือก **Free** (ฟรี)
4. กด **"Create new project"**
5. **รอให้สร้างเสร็จ** (ประมาณ 2-3 นาที)
   - จะเห็นข้อความ "Setting up your project..."

### 1.4 ดู Connection Information
1. หลังจาก Project สร้างเสร็จแล้ว
2. ไปที่ **Settings** (ไอคอน ⚙️ ด้านซ้าย)
3. เลือก **Database** (ในเมนูด้านซ้าย)
4. หา **Connection string** หรือ **Connection info**
5. **คัดลอกข้อมูลต่อไปนี้** (จะใช้ตอนตั้งค่า Streamlit Cloud):
   - **Host**: `db.xxxxx.supabase.co` (ตัวอย่าง)
   - **Port**: `5432`
   - **Database name**: `postgres`
   - **User**: `postgres`
   - **Password**: (รหัสผ่านที่ตั้งไว้ในขั้นตอน 1.3)

**💡 Tip:** เปิด Notepad หรือเอกสารไว้ แล้วคัดลอกข้อมูลเหล่านี้เก็บไว้

---

## 📋 ขั้นตอนที่ 2: Push โค้ดไป GitHub (ถ้ายังไม่มี)

### 2.1 ตรวจสอบว่าโค้ดอยู่ใน GitHub แล้วหรือยัง
- ถ้ามีแล้ว → ข้ามไปขั้นตอนที่ 3
- ถ้ายังไม่มี → ทำตามขั้นตอน 2.2

### 2.2 Push โค้ดไป GitHub
1. เปิด Terminal หรือ Command Prompt
2. ไปที่โฟลเดอร์โปรเจค:
   ```bash
   cd F:\PROJECT_DEV\POS
   ```
3. ตรวจสอบว่าเป็น Git repository:
   ```bash
   git status
   ```
4. ถ้ายังไม่ใช่ Git repository:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```
5. สร้าง Repository บน GitHub:
   - ไปที่ https://github.com
   - กด **"New repository"**
   - ตั้งชื่อ repository (เช่น `pos-system`)
   - กด **"Create repository"**
6. Push โค้ด:
   ```bash
   git remote add origin https://github.com/username/pos-system.git
   git branch -M main
   git push -u origin main
   ```
   (แทน `username` และ `pos-system` ด้วยชื่อจริง)

---

## 📋 ขั้นตอนที่ 3: Deploy บน Streamlit Cloud (5 นาที)

### 3.1 ไปที่ Streamlit Cloud
1. เปิดเบราว์เซอร์
2. ไปที่ **https://share.streamlit.io**
3. **Sign in** ด้วย GitHub account (ต้องใช้ account เดียวกับที่ push โค้ด)

### 3.2 สร้าง App ใหม่
1. หลังจาก Sign in แล้ว จะเห็นหน้า Dashboard
2. กดปุ่ม **"New app"** (มุมขวาบน)
3. ตั้งค่า:
   - **Repository**: เลือก repository ที่มีโค้ดของคุณ
   - **Branch**: `main` หรือ `master` (ตามที่ใช้)
   - **Main file path**: `app.py`
   - **App URL**: (จะสร้างอัตโนมัติ หรือตั้งชื่อเองได้)
   - **Python version**: เลือก **3.11** หรือ **3.10**

### 3.3 ตั้งค่า Secrets (สำคัญมาก!)
1. ก่อนกด "Deploy" ให้กด **"Advanced settings"** ก่อน
2. เลื่อนลงไปหา **"Secrets"** tab
3. กด **"Secrets"** tab
4. จะเห็นช่องข้อความสำหรับใส่ secrets
5. **คัดลอกและวาง** ข้อมูลต่อไปนี้ (ใช้ข้อมูลจาก Supabase ที่คัดลอกไว้):
   ```toml
   [database]
   type = "postgresql"
   host = "db.xxxxx.supabase.co"
   port = 5432
   user = "postgres"
   password = "your-password-here"
   database = "postgres"
   ```
6. **แก้ไขข้อมูลให้ถูกต้อง:**
   - แทน `db.xxxxx.supabase.co` ด้วย **Host** จาก Supabase
   - แทน `your-password-here` ด้วย **Password** จาก Supabase
   - ตรวจสอบว่า **port**, **user**, **database** ถูกต้อง

**⚠️ ตัวอย่างที่ถูกต้อง:**
```toml
[database]
type = "postgresql"
host = "db.abcdefghijklmnop.supabase.co"
port = 5432
user = "postgres"
password = "MySecurePassword123!"
database = "postgres"
```

### 3.4 Deploy!
1. ตรวจสอบข้อมูล secrets อีกครั้ง
2. กดปุ่ม **"Deploy!"** (มุมล่างขวา)
3. **รอให้ build เสร็จ** (ประมาณ 2-5 นาที)
   - จะเห็น progress bar
   - ถ้ามี error จะแสดงใน logs

### 3.5 ตรวจสอบผลลัพธ์
1. หลังจาก build เสร็จแล้ว
2. จะเห็น URL ของ app (เช่น `https://your-app.streamlit.app`)
3. กด URL เพื่อเปิด app
4. ลองใช้งาน:
   - ล็อคอิน (admin/admin)
   - เพิ่มสินค้าทดสอบ
   - ตรวจสอบว่าข้อมูลถูกบันทึก

---

## 📋 ขั้นตอนที่ 4: ตรวจสอบว่าเชื่อมต่อ Database ได้ (2 นาที)

### 4.1 ตรวจสอบใน App
1. เปิด app ที่ Streamlit Cloud
2. ล็อคอินด้วย admin/admin
3. ไปที่หน้า **📦 จัดการสต็อค**
4. เพิ่มสินค้าทดสอบ (เช่น "สินค้าทดสอบ", ราคา 100)
5. บันทึก

### 4.2 ตรวจสอบใน Supabase
1. ไปที่ Supabase Dashboard
2. ไปที่ **Table Editor** (เมนูด้านซ้าย)
3. ดูตาราง `products`
4. **ควรเห็นสินค้าที่เพิ่มไว้!** ✅

### 4.3 ถ้าเห็นข้อมูล = สำเร็จ! 🎉
- ✅ ระบบเชื่อมต่อ database ได้แล้ว
- ✅ ข้อมูลจะเก็บถาวร
- ✅ ไม่หายเมื่อ app restart

---

## 🔧 แก้ไขปัญหา

### ปัญหา: Build ล้มเหลว
**สาเหตุ:** 
- Dependencies ไม่ครบ
- โค้ดมี error

**วิธีแก้:**
1. ดู logs ใน Streamlit Cloud
2. ตรวจสอบ `requirements.txt`
3. แก้ไข error
4. Push ใหม่

### ปัญหา: ไม่สามารถเชื่อมต่อ Database
**สาเหตุ:**
- Secrets ไม่ถูกต้อง
- Password ผิด
- Host ผิด

**วิธีแก้:**
1. ตรวจสอบ secrets อีกครั้ง
2. ตรวจสอบ password จาก Supabase
3. ตรวจสอบ host จาก Supabase
4. แก้ไข secrets แล้ว redeploy

### ปัญหา: ข้อมูลไม่แสดง
**สาเหตุ:**
- Database ยังไม่มีตาราง
- Migration ยังไม่รัน

**วิธีแก้:**
1. ระบบจะสร้างตารางอัตโนมัติเมื่อ app start
2. รอสักครู่แล้ว refresh
3. ตรวจสอบ logs

---

## ✅ Checklist

ทำตาม checklist นี้เพื่อให้แน่ใจว่าทุกอย่างถูกต้อง:

- [ ] สร้าง Supabase Project สำเร็จ
- [ ] คัดลอก Connection info จาก Supabase
- [ ] Push โค้ดไป GitHub
- [ ] สร้าง App บน Streamlit Cloud
- [ ] ตั้งค่า Secrets ถูกต้อง
- [ ] Deploy สำเร็จ
- [ ] App ทำงานได้
- [ ] ข้อมูลถูกบันทึกใน Supabase

---

## 💡 Tips

### 1. **สำรองข้อมูล**
- ไปที่ Supabase Dashboard
- **Settings** > **Database**
- กด **"Backup"** (มีให้ฟรี!)

### 2. **ตรวจสอบ Usage**
- ไปที่ Supabase Dashboard
- ดู **Usage** เพื่อตรวจสอบว่าใช้ database เท่าไหร่
- Free tier: 500MB (พอใช้มาก!)

### 3. **Security**
- อย่า commit secrets ไป GitHub
- ใช้ Streamlit Secrets เท่านั้น
- ใช้ strong password

---

## 📊 สรุป

### ✅ สิ่งที่ได้:
- **App บน Streamlit Cloud** - ฟรี
- **Database บน Supabase** - ฟรี (500MB)
- **ข้อมูลถาวร** - ไม่หายเมื่อ restart
- **ฟรี 100%** - ไม่มีค่าใช้จ่าย

### ⏱️ ใช้เวลา:
- **10-15 นาที** สำหรับตั้งค่าครั้งแรก
- **0 นาที** สำหรับการใช้งานต่อๆ ไป

### 💰 ราคา:
- **ฟรี 100%** - ไม่มีค่าใช้จ่าย

---

## 🎉 เสร็จแล้ว!

ตอนนี้ระบบ POS ของคุณ:
- ✅ ทำงานบน Streamlit Cloud (ฟรี)
- ✅ เก็บข้อมูลใน Supabase (ฟรี)
- ✅ ข้อมูลถาวร (ไม่หาย)
- ✅ ฟรี 100%

**Happy Coding! 🚀**

