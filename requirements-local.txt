# Requirements for Local Development
# ใช้ไฟล์นี้สำหรับติดตั้ง dependencies บน localhost
# ติดตั้งด้วย: pip install -r requirements-local.txt

# Core dependencies
streamlit>=1.28.0
sqlalchemy>=2.0.0
pandas>=2.0.0
plotly>=5.17.0
reportlab>=4.0.0
bcrypt>=4.0.0
openpyxl>=3.1.0
python-dateutil>=2.8.0
python-barcode>=0.15.1
Pillow>=10.0.0
numpy>=1.24.0
qrcode[pil]>=7.4.2

# Local-only dependencies (ไม่รองรับบน Streamlit Cloud)
# สำหรับสแกนบาร์โค๊ดด้วยกล้อง
pyzbar>=0.1.9
streamlit-camera-input-live>=0.2.0

# Optional: สำหรับ image processing (ถ้าต้องการ)
# opencv-python>=4.8.0

